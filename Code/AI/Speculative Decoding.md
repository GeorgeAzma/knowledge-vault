https://www.adaptive-ml.com/post/speculative-decoding-visualized
- $S$ smaller model `draft`
- $L$ larger model `target`
- $i$ token index
- $P_{S_i}$ probability distribution over vocabulary tokens generated by $S$ for $i$-th token
- $t_i\sim P_{S_i}$ token $t_i$ actually generated/sampled by $S$ as $i$-th token

1. Generate probability distributions over vocabulary for each token using smaller model $P_{S_i}$
2. Input actual generated tokens $t$ into $L$, which calculates 
`TODO:`
