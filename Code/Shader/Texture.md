``` python
texelFetch
texelFetchOffset

textureLod
textureLodOffset

textureOffset

textureGather(sampler, uv, comp: i32) # comp: component index (xyzw = 0123)
textureGatherOffset
textureGatherOffsets
textureGatherCompare

textureSamples
textureSize

textureGrad
textureGradOffset

textureProj
textureProjGrad
textureProjGradOffset
textureProjLod
textureProjLodOffset
textureProjOffset

textureQueryLevels
textureQueryLod

textureNumLayers
textureNumSamples
```