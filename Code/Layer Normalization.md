Calculate [[Mean]] and [[Variance]] for each [[Neural Network]] layer 
across each feature vector in a batch, feature vectors 
for [[Convolutional Neural Network]] are 3 R,G,B channels
![[Layer Normalization.webp]]
Modify outputs so that $\mu=0$ and $\sigma=1$
