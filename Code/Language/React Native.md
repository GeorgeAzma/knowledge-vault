``` bash
# enable VT-x or VT-d in bios for android emulator
# install android studio
npx create-expo-app@latest
cd my-app
npm run web/android/ios
# install expo phone app to see app on your phone
```
### Build
``` bash
npm install g eas-cli
eas build -p android
```
