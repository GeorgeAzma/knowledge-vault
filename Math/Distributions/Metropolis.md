same as [[Metropolis-Hastings]] but assumes symmetric proposal distribution $q(x'\mid x)=q(x\mid x')$
- so chance from walking $x\to x'$ is same as $x'\to x$ `direction does not matter`
- imagine $x\to x'$ random walk chance being distance based $q(x'\mid x)=f(|x-x'|)$
